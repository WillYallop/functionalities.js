export default class FormValidation {
    constructor() {
        console.log('I am form validation!');

        // Create a field class.
        // Depending on fields in the form, extend this class with a specific input type.

    }
}