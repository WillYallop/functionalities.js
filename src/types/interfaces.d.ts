interface ApplyBasicStyles {
    slider: HTMLElement,
    wrapper: HTMLElement,
    slides: any
}

interface AdjustSlides {
    perPage: number,
    direction: 'vertical' | 'horizontal',
    gap: number,
    slider: HTMLElement,
    wrapper: HTMLElement,
    slides: any
}

// Form Validation
// Inputs map
